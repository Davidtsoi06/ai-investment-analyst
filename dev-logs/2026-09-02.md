# 2026-09-02 开发日志（v1.0.5）

## 完成事项：v1.0.5 全部 11 项修改（用户确认执行）

### 1. 持仓数据源去掉 finance.db 直读 → 双模式（快照文件 / 手动录入）
- backend/app/data_sources/portfolio_app.py：删除 DB_PATH/detect_db/_readonly_conn/read_snapshot_db，只保留快照文件读取（detect_snapshot_folder 只探测 data/portfolio）
- backend/app/services/portfolio_sync.py：新增 get_mode/set_mode（system_settings portfolio.source_mode，默认 snapshot）；status 返回 {mode, snapshot_detected, snapshot_dir, snapshot_modified_at, holdings_count}；upsert_manual_holding / delete_manual_holding
- main.py：/api/portfolio/mode GET+PUT、POST/DELETE /api/portfolio/holdings；overview 按当前模式过滤（source）
- review_agent：删除 _asset_map 与 finance.db 净值兜底（只依赖快照）；头注释更新
- 8 处读取 holdings 的模块（recommend/news/chat/review/summary/risk/research_prompt/eastmoney_research）按模式过滤 source
- 冒烟验证（API 全流程：模式切换/手动 CRUD/overview 过滤/sync 缺快照提示）通过
- 前端：Settings「持仓数据来源」二选一卡片；Portfolio 页重写（模式切换/手动录入表单/删除/快照状态）
- 诊断确认：理财软件 aiPortfolio.folder 已指向 %APPDATA%\ai-investment-analyst\data\portfolio（默认目录方案无损）

### 2. 导航「仪表盘」→「主页」（AppLayout + Dashboard h1）

### 3. 版本统一真实版
- 删除 AppLayout APP_VERSION='0.6.0' 硬编码；新增 effect 经 window.updater.getVersion()（主进程 app.getVersion()）取真实版本，preload/浏览器兜底 dev
- Settings 关于栏同改（updVersion || appInfo || dev）

### 4. 推荐中心
- 后端 generate_recommendations 返回 candidate_count/pool_size/empty_reason（无推荐原因文案）
- 前端：空推荐显示原因 + 规则降级/缓存说明（title+文案）+ cached 时按钮「重新生成（覆盖今日）」

### 5+6. 风险指标：通俗解读 + 数据修复
- Risk.tsx MetricCard 增加 explain 字段：7 张指标卡均有白话解读
- 修复明细 40220.0%：weight_pct ?? value → weight ?? weight_pct（value 市值不再当 %）；api.ts 补 weight 类型
- 修复 market_share [object Object]：转数组取最大 + 各市场占比 hint
- 预警列表按类型格式化（集中度/占比/回撤=%，Beta/夏普=数值，VaR=金额）
- 后端 stress estimated_loss_pct 统一为小数（0.6 不再显示成 60%）；renderDetail 数组分支金额显示

### 7. fmtPct 乘 100 bug
- lib/format.ts fmtPct 删除自动 ×100（后端全部比率字段已是百分数原值），删 toPercent；Dashboard 指数 -0.58 → -0.58%

### 8. 问答主动外部搜索
- 新增 backend/app/data_sources/market/stock_search.py（东财 suggest 名称→代码；过滤权证/基金；港股 MktNum=116）
- chat_agent：_extract_symbols(allow_external) + _guess_stock_keywords/_search_external；build_context 搜到股时「综合」→「个股分析」
- chat_prompt SYSTEM_PROMPT 增加防空答规则（不回复「无相关数据」）
- 实测：『泡泡玛特怎么样』→ 09992 行情+指标；『小米集团现在能买吗』→ 01810 ✅

### 9. Chat 布局占满 + 历史查看
- 根容器 h-[calc(100vh-3.5rem)] flex-col；聊天/研报/历史各滚动区 flex-1 min-h-0（消除底部空白）
- 历史条目点击载入完整问答（question+answer），保留「重新提问」

### 10. 设置页投资风格可编辑
- 新增「投资风格」卡片：风险偏好（保守/稳健/激进，带说明）+ 金额/周期/经验下拉，PUT /api/profile

### 11. × = 完全退出
- window.ts 删除 close→托盘逻辑；index.ts window-all-closed → app.quit()（before-quit 停后端）；tray「显示主窗口」无窗口时重建；最小化保持后台

## 版本与文档
- frontend/desktop package.json、backend config.py → 1.0.5
- CHANGELOG.md 新增 [1.0.5]；README.md 全面更新（功能表/技术栈/进度/持仓对接说明）

## 验证
- backend compileall OK；frontend typecheck OK；desktop tsc OK
- 冒烟：portfolio 双模式 API 全流程 OK；问答外部搜索 OK

## 待办 / 下一步
- 待用户确认后：构建（PyInstaller onedir + vite build + electron-builder NSIS）→ 本地验证 → 打 tag v1.0.5 → GitHub Release 上传（先删同 tag 旧 Release，再传 exe/blockmap/latest.yml，注意 uploads.github.com）
