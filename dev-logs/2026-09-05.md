# 2026-09-05 开发日志（v1.0.7：API Key 连接稳定性）

## 背景
用户反馈：Key 测试可行、保存成功，但正式使用仍显示规则降级/无 Key（黑盒：解密失败、401、限流等全部静默）。

## 实施（方案经用户确认，照搬理财软件机制）
### 后端
- crypto_util.py：解密失败记录原因（get_last_error()），密钥文件缺失/重建有日志提示（不再静默空 Key）
- settings_service.py：删除 settings.deepseek_api_key 内存缓存；get_ai_key() 每次实时解密（与理财软件一致）；新增 ai_key_tail()
- llm_client.py：统一 _record_ai_error/_clear_ai_error（system_settings 'ai_last_error'，JSON {at,error}）；
  - chat/test_connection 失败抛可读中文原因（401 Key 无效/402 余额/429 限流等 + 服务端原文 ≤200）
  - 成功自动清除历史错误；未配置 Key 也记录"未配置"原因
  - 新增 ai_status()：{configured, key_tail, crypto_error, last_error, last_error_at}（不含明文）
- main.py：POST /api/settings/ai-key 保存成功后清错误；新增 GET /api/settings/ai-status

### 前端
- Settings：AI 卡片重做——状态徽章（已配置 sk-****尾号/未配置）、异常原因条（crypto/last_error）、「保存并测试」一体化按钮、申请链接（主进程 shell.openExternal，desktop ipc 'app:openExternal' + preload window.app + 类型）
- Recommendation：source=rules 且存在 AI 错误/未配置时，今日卡顶部显示失败原因与「去设置」链接

## 验证
- 后端编译 OK；前端/desktop typecheck OK
- 冒烟：save 后 ai-status key_tail 实时变化；坏 key 401 → ai-status 显示完整错误与时间（真实链路验证通过）

## 待办
- 待用户确认构建 → v1.0.7 安装包 → 用户安装验证 → 推送 Release
