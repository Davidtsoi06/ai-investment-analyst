# S10 AI 股票推荐模块 — 端到端验证报告

- 验证人：verifier（S10 团队验证工程师）
- 验证日期：2026-08-31
- 验证范围：后端 API 实测 + 技术指标/约束规则/回测逻辑单元验证 + 前端类型检查/构建接入 + 前后端契约一致性
- 结论：**全部通过（PASS）**，可进入集成评审

---

## 1. 验证环境

| 项 | 值 |
|---|---|
| 后端 | FastAPI 0.141.1 / uvicorn 0.52.4 / Python 3.10.11（backend/.venv） |
| 前端 | React + TS / Vite 8.2.2 / Node v24.18.1 / npm 11.16.0 |
| 数据库 | SQLite data/ai_invest.db（WAL，SCHEMA_VERSION 2 迁移已应用） |
| 服务 | 127.0.0.1:8756，两种模式实测：无令牌（开发）与 BACKEND_TOKEN 令牌模式 |

## 2. 后端 API 实测结果（全部 HTTP 实测）

| # | 接口 | 结果 | 关键证据 |
|---|---|---|---|
| 1 | GET /api/health | ✅ 200 | {"status":"ok","version":"0.4.0"} |
| 2 | POST /api/recommend/run | ✅ 200 | ok:true, cached:false, source:"rules"，items 4 条长线（中国平安/招商银行/美的集团/长江电力，均含估值区间 valuation_min/max、置信度 77~85、逻辑、风险等级），blocked 1 条（600519 资金匹配拦截），errors:[] |
| 3 | GET /api/recommend/today | ✅ 200 | 已生成 → cached:true 返回缓存；未生成 → 自动触发生成（force=False 语义验证） |
| 4 | GET /api/recommend/history?limit= | ✅ 200 | 返回数组，首条含 outcome/result_pct/result_price/eval_days/status，按 rec_date 倒序 |
| 5 | GET /api/recommend/backtest | ✅ 200 | 结构 {summary, by_type, by_month, recent}；summary 含 count/win_rate/avg_return/total_return/wins/losses/stops/flats；by_type 含短线/长线两组；by_month 近 6 月；recent 明细 20 条 |
| 6 | POST /api/recommend/backtest/evaluate | ✅ 200 | {"evaluated":0,"skipped":[{id,symbol,reason:"推荐日当天不结算"}×4]}——当日推荐不结算，符合设计 |
| 7 | 401 回归 | ✅ 6/6 | 6 个接口 ×（无 token→401 / 错误 token→401 / 正确 token→200）全部符合 |

补充：契约别名 /api/recommendations/{generate,today,history,performance} 与 /api/recommend/* 等价（generate 附 existing 字段），代码核对一致。

## 3. 技术指标库单元验证（纯函数，30 项断言全过）

SMA / EMA（种子收敛）/ MACD（DIF>DEA 上行、金叉事件）/ RSI（Wilder，单边 100/0 边界）/ KDJ / BOLL（上>中>下）/ cross_above（金叉真伪）/ vol_ratio / is_breakout（放量突破 hit+量比 5.0）/ ma_status（多/空/纠缠）/ 周月 K 聚合 / pct_change / indicator_snapshot（30 字段齐全）/ score_short_term / score_long_term —— **全部断言 PASS**。

## 4. 约束规则单元验证（4 分支全拦截，独立临时库验证）

| 约束 | 用例 | 结果 |
|---|---|---|
| 市场匹配 | AAPL(美股) 画像仅 A股/港股 → 拦截 | ✅ |
| 持仓冲突 | 600519 在持仓 → 拦截 | ✅ |
| 资金匹配 | 单手价 > 可投资金额 10% → 拦截（实测 600519 被拦截：单手价约 129426 元） | ✅ |
| 风险匹配 | 稳健型画像 + 高风险推荐 → 拦截 | ✅ |
| 通过路径 | 600036 全部满足 → 放行 | ✅ |
| 解析 | parse_invest_amount（10-50万→10万/1亿）/ lot_price / risk_match | ✅ |

## 5. 回测结算与统计验证（独立临时库 + mock K 线 provider，不污染开发库）

- 结算规则：止损优先于目标（stop -9.52%）、触目标 win（+8.91%）、窗口走完按末日收盘 loss（-8.06%）/ flat（0%）—— 4 种 outcome 全验证 ✅
- 统计：count=4、win_rate=25.0%、avg_return=-2.17、total_return=-8.67、by_type（短线 2/长线 2）、by_month（2026-08）、recent 字段齐全 ✅
- 幂等：二次 evaluate 返回 evaluated=0 ✅
- 当天推荐不结算（推荐日当天跳过）✅

## 6. 前端验证

| 项 | 结果 |
|---|---|
| npm run typecheck（tsc --noEmit） | ✅ 0 错误 |
| npm run build（vite build） | ⚠️ 沙箱内 EPERM（vite 配置加载阶段 spawn 子进程被 DSH 沙箱拦截，属验证环境限制非代码问题）；dist 最新产物（13:02:57）已含推荐中心代码佐证构建链路正常 |
| 页面实现核对 | ✅ Recommendation.tsx：短线/长线分区卡片（入场区间/止损/目标价 vs 估值区间/当前价）、置信度三色进度条、风险徽章（保守=success/稳健=info/激进=danger）、logic line-clamp-3、AI/规则 source 徽章、缓存标记、约束拦截列表展示、回测报告（6 指标 + 分组 + 月度 + 明细 + 结算按钮）、历史列表 |
| 前后端契约一致性 | ✅ 前端 api.ts 5 个接口路径/字段与后端实测响应完全对齐（含 valuation_min/max、blocked/errors、outcome/result_pct、summary/by_type/by_month/recent） |

## 7. 定时任务验证

- ✅ register_recommend_jobs 注册 recommend_daily 09:15（仅交易日，is_trading_day 过滤，replace_existing 幂等）
- ✅ 日志确认：`已注册定时任务 recommend_daily: 9:15`、`推荐定时任务已注册（09:15 每日，仅交易日）`
- ✅ 调度器 APScheduler BackgroundScheduler 北京时间（Asia/Shanghai）

## 8. 数据库验证

- ✅ recommendations 表：id/symbol/name/market/rec_type/entry_min/entry_max/stop_loss/target/confidence/logic/risk_level/rec_date/rec_price/status + valuation_min/valuation_max（迁移 v2）—— 16 列齐全
- ✅ recommendation_performance 表：recommendation_id/result_price/result_pct/outcome/entry_price/eval_days/horizon/evaluated_at —— 8 列齐全
- ✅ 迁移 v2 已应用（app.log 无迁移报错），索引 idx_recommendations_rec_date / idx_recommendations_status / idx_rec_perf_rec_id 存在
- ✅ force 语义：同日重复 run 覆盖当日 open 推荐（DELETE+INSERT），不堆积重复数据

## 9. AI 路径说明

当前环境无 DeepSeek API Key，实测走规则引擎降级路径（source="rules"）。AI 路径代码审查确认：_ai_configured() → 双 prompt 生成 → _sanitize_ai_item 字段校验（短线要求 entry_min<entry_max、止损<入场、目标>入场；长线要求估值区间合法；confidence 0-100 裁剪；risk_level 白名单）→ 失败降级 rules → 约束过滤 → 入库，source 字段正确区分 'ai'/'rules'。

## 10. 合规检查

- ✅ README.md 免责声明存在：「AI 分析、推荐、预测内容仅供参考，不构成任何投资建议」
- ✅ 未触碰 finance.db（个人理财软件数据库只读规则），验证全程使用 ai_invest.db 与独立临时库
- ✅ 验证临时文件已清理

## 11. 遗留风险（非阻断）

1. AI 生成路径需配置 DeepSeek Key 后做一次真实链路冒烟（当前仅代码审查 + 降级实测）。
2. 前端 vite build 在本验证沙箱内受 EPERM 限制未重跑成功，但 typecheck 零错误 + dist 最新产物已含推荐中心代码；建议在正常终端复跑一次 npm run build 确认。
3. 真实回测结算需交易日窗口（短线 5 日 / 长线 20 日）自然触发，已用 mock provider 验证结算逻辑正确性。
